<%= form_for(fail) do |f| %>
  <% if fail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(fail.errors.count, "error") %> prohibited this fail from being saved:</h2>

      <ul>
      <% fail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url %>
  </div>

  <div class="actions">
    <%= f.submit "Confirmer", class: "waves-effect waves-light btn" %>
  </div>

  <%= link_to 'Retour', fails_path %>
<% end %>
